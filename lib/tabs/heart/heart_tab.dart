import 'package:flutter/material.dart';

class HeartTab extends StatelessWidget {
  const HeartTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('heart'),);
  }
}
